# Unused image archive

This folder contains source, preview, intermediate, and currently unused image
files moved out of the runtime asset folders.

Godot ignores this directory through `.gdignore`.
